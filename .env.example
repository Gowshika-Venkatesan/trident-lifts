# Connect the enquiry form to your CRM, serverless function, or form provider.
# The endpoint must accept a JSON POST request.
VITE_FORM_ENDPOINT=

# Secure admin portal database and authentication.
# Use the public anonymous key only. Never place the service-role key here.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
